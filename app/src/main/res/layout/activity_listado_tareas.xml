<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/list_screen_background">

    <TextView
        android:id="@+id/textoVacio"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="No hay tareas disponibles"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="@color/empty_state_color"
        android:layout_centerInParent="true"
        android:visibility="gone"/>

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/toolbar_background"
        android:elevation="4dp"
        android:theme="@style/ThemeOverlay.TareaLarga1trimestre.Toolbar"
        app:popupTheme="@style/ThemeOverlay.TareaLarga1trimestre.PopupMenu" />


    <!-- Botón flotante para añadir tareas -->

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerTareas"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/toolbar"
        android:layout_marginTop="-1dp"
        android:clipToPadding="false"
        android:paddingBottom="96dp" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/btnAgregarTarea"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_margin="24dp"
        android:contentDescription="Añadir tarea"
        app:srcCompat="@android:drawable/ic_input_add"
        app:tint="@color/toolbar_content"
        app:backgroundTint="@color/toolbar_background" />

</RelativeLayout>
